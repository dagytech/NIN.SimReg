CREATE TABLE nida_mock_records (
    nin VARCHAR(255) PRIMARY KEY,
    first_name VARCHAR(255),
    middle_name VARCHAR(255),
    last_name VARCHAR(255),
    date_of_birth DATE,
    fingerprint_template VARCHAR(255)
);
